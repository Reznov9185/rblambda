source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Uses this gem for zipping the directory for deploying
gem 'rubyzip', '>= 1.0.0' # will load new rubyzip version
